function main(): number {
    let input: string =
``;

    let inputRows = input.split("\n");

    inputRows.forEach(row => {
        
    });

    function findAnswer(): number {
       let count = 0;

        return count;
    }

    // return countVisible();
    return findAnswer();
}

console.log(main());
