namespace blah {
    let input: string =
``;

    let inputRows = input.split("\n");

    inputRows.forEach(row => {
        
    });

    function findAnswer(): number {
       let count = 0;

        return count;
    }

    console.log(findAnswer());
}